- return if @challenge_count == 0
%p.challenges
  %strong Challenges:
  %span.no_challenges_completed
    There
    = @challenge_count > 1 ? 'are' : 'is'
    = pluralize(@challenge_count, 'challenge')
    for you to do.
    = link_to('Jump to challenges >', '#challenge1')
  %span.challenges_completed
    You've completed
    %span.completed_challenges_count
    out of
    = "#{pluralize(@challenge_count, 'challenge')}."
    = link_to('Jump to challenges >', '#challenge1')
