#examples
  %h1 The Gallery

  %p Explore the gallery of drawings, animations and games below.
  %p Each example contains the code used to create it. Tweak it, play with it, learn from it.

  - @sections.each do |section|
    .section.row.my-3
      .col-xs-12.mb-1
        %h2= section
      - @examples[section].in_groups_of(3).each_with_index do |example_group, index|
        .group{class: (index > 0 ? 'hidden' : '')}
          - example_group.compact.each do |example|
            .col-xs-12.col-md-6.col-lg-4
              %a.card.example{href: example_path(example)}
                = image_tag("examples/#{example}.png", class: 'card-img-top img-fluid', alt: example, title: example)
                .card-block
                  .card-text
                    = render partial: "examples/info/#{example}_details"
                  .card-footer
          - if @examples[section].length > (index * 3)
            .row
              .col-xs-12.text-xs-center.pt-1
                %button.btn.btn-primary View More
